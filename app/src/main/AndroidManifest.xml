<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hfad.best4pets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".kontakt_activity"></activity>
        <activity android:name=".infa_dostavka_activity" />
        <activity android:name=".HistoryDetailsActivity" />
        <activity android:name=".ZakazInfoActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".EnteredActivity" />
        <activity android:name=".BookDetailsActivity" />
        <activity android:name=".Katalog" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>